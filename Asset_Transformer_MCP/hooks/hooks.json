{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/bootstrap.py\" 2>/dev/null || python \"${CLAUDE_PLUGIN_ROOT}/bootstrap.py\" 2>/dev/null || py -3 \"${CLAUDE_PLUGIN_ROOT}/bootstrap.py\" 2>/dev/null || echo \"Unity Asset Transformer MCP: no Python 3 interpreter found on PATH - install Python 3.12 (https://www.python.org/downloads/) and start a new Claude Code session to set up the Asset Transformer MCP server.\"",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
